import {cn} from '@/lib/utils'; import type {PipelineTemplateStage} from '@/integrations/directus/pipelineTemplates'; export function TemplatePreview({stages,compact=false}:{stages:PipelineTemplateStage[];compact?:boolean}){return <div className={cn('flex gap-1.5 overflow-x-auto pb-1',compact?'min-w-0':'min-w-[420px]')}>{stages.map((s,i)=><div key={`${s.name}-${i}`} className="flex-1 min-w-[92px] rounded-md border bg-muted/40 p-2" style={{borderTop:`3px solid ${s.color}`}}><div className="text-xs font-medium truncate">{s.name}</div><div className="text-[11px] text-muted-foreground mt-1">{s.probability}%</div></div>)}</div>}
